#include <bits/stdc++.h>
using namespace std;
int main()
{
	int n,m;
	char x,y,a[26];
	for(int i=0;i<26;i++)
		a[i]='a'+i;
	string s;
	cin >> n >> m;
	cin >> s;
	while(m--)
	{
		cin >> x >> y;
	}
	cout << s;
	return 0;
}